services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=forumdb'
      - 'POSTGRES_PASSWORD=admin123'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
