server:
  port: 8080

spring:
  application:
    name: forum-service

  datasource:
    #url: jdbc:postgresql://95.6.82.50:5432/forumdb
    #username: sosyal-plaka-db-mngr
    #password: Ayro1234+
    #driver-class-name: org.postgresql.Driver

    url: jdbc:postgresql://localhost:5432/forumdb
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate.format_sql: true
  sql:
    init:
      mode: always


  # Redis disabled for local development
  cache:
    type: none
  session:
    store-type: none
    
management:
  endpoints:
    web:
      exposure:
        include: health,info

